TODO: Explain how to install the addon and different settings.  
Make sure to add the security notice about PathMap here!  
  
Show what settings are inside the project settings AND where to find the internal CVars!  
Since the system is dogfed the user must be able to find the kibble (lol) resources so they can edit it to their liking.