TODO: Document commands

Should touch:

- Executing commands
- Default command sets and their features
- Lifecycle of command sets (what does context based mean)
- Creating a command set (can be funny, like "This is probably what you're looking for")
-   - Lifecycle
-   - Using Godot methods (The wrappers for `_EnterTree`, `_Ready`, `_ExitTree`)
-   - Parsing arguments
-   -   - Manual parsing (with code example)
-   -   - Using `ArgParser` (with code example)
- Command to command, and command to alias collision reconciliation (Command first in (self diagnostic, easy error), alias killed for command (also self diagnostic))
